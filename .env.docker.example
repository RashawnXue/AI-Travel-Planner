# Docker 部署环境变量配置模板（前端）
# 复制此文件为 .env.docker 并填入实际值

# ============================================
# Supabase 配置
# ============================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# ============================================
# 阿里云百炼配置
# ============================================
# 注意: API Key 不应包含在镜像中，需要通过其他安全方式注入或在界面配置
# VITE_BAILIAN_API_KEY - 不要在此配置，使用运行时注入或用户界面配置

VITE_BAILIAN_APP_ID=your_bailian_app_id_here

# ============================================
# 高德地图配置
# ============================================
VITE_AMAP_KEY=your_amap_key_here

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件: cp .env.docker.example .env.docker
# 2. 填入实际配置值
# 3. 构建镜像: ./build.sh
# 4. 运行容器: docker run -d -p 80:80 ai-travel-planner
#
# 注意事项:
# - 此文件包含敏感信息，请勿提交到版本控制
# - API 密钥应该通过运行时环境变量或密钥管理服务注入
# - OSS、ASR 等敏感配置已移至后端，不再需要在前端配置
# - 后端配置请参考 AI-Travel-Planner-be/.env.example
